const num1 = 100;
const num2 = 50;
let val;

//simple math with numbers

val = num1 + num2;
val = num1 * num2;
val = num1 - num2;
val = num1 / num2;
val = num1 % num2;

//Math Object

val = Math.PI;
val = Math.round(2.8); // round a number
val = Math.ceil(2.4); // round up ( it will give 3)
 val = Math.floor(2.8); // round the value down (2).(also give the number in integer.)
 val = Math.sqrt(64); // WE WILL GIVE 8
 val = Math.abs(-3); // give the positive value of it.(3).
 val = Math.pow(8,2);  // it will give 8th power of 2.
 val = Math.min(2,33,4,1,55,6,3); // give minimum number.
 val = Math.max(2,33,4,1,55,6,3,-2); // give maximum number.
 val = Math.random(); // It will give you a random number.

 val = Math.floor(Math.random() * 20); // 20 tak ka random number dega.(20 tak ke integer number dega).
// console.log(val);
